import(utils)
import(Biobase)
import(nlme)
import(emmeans)
import(limma)
import(methods)
import(fftw)
importFrom("graphics", plot)

export(qusage,
       qgen,
       makeComparison,
       aggregateGeneSet,
       calcVIF,
       oneWay.pVal,
       pdf.pVal,
       twoWay.pVal,
       twoCurve.pVal,
       getXcoords,
       calcBayesCI,
       newQSarray,
       qsTable,
       numFeatures,
       numPathways,
       plotGeneSetDistributions,
       plotDensityCurves,
       plotCIs,
       plotCIsGenes,
       read.gmt,
       combinePDFs,
       plotCombinedPDF
      )

exportClasses(QSarray)
exportMethods(plot, print, dim, head, summary, "[<-")